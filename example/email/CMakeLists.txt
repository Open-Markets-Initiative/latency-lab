set (EMAIL_BINARY email)
set (Boost_LIBRARIES Boost::program_options Boost::filesystem Boost::system)
add_executable(${EMAIL_BINARY} main.cpp)
target_link_libraries(${EMAIL_BINARY} PRIVATE ${Boost_LIBRARIES})
target_link_libraries(${EMAIL_BINARY} PRIVATE stdc++fs)