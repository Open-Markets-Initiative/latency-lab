set(MATCHING_BINARY matching)
set (Boost_LIBRARIES Boost::program_options Boost::filesystem Boost::system)
add_executable(${MATCHING_BINARY} main.cpp)
target_link_libraries(${MATCHING_BINARY} PRIVATE ${Boost_LIBRARIES})
target_link_libraries(${MATCHING_BINARY} PRIVATE stdc++fs)
